import tweetsRouter from '../routes/tweets.routes';
import membersRouter from '../routes/members.routes';
import authRouter from '../routes/auth.routes';

export { tweetsRouter, membersRouter, authRouter }